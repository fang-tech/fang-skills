# {ClassName}

📁 **源码位置**: `{full/path/to/ClassName.java}`

## 概述
{这个类是做什么的，在项目中扮演什么角色}

## 继承/实现关系
```
{父类/接口} ← {ClassName}
```

## 关键方法

| 方法 | 行号 | 作用 | 阅读优先级 |
|------|------|------|------------|
| `{method()}` | L{start}-L{end} | {一句话描述} | ⭐/📖/📋 |

## {核心方法名}() 详解

<!-- 前置知识: {列出理解此方法需要的前置知识} -->

**调用链**（建议对照源码阅读）：
1. `L{n}`: {做了什么} → 调用 `{OtherClass}`（见 [{OtherClass}.md]）
2. `L{n}`: {做了什么}
3. `L{n}`: {做了什么}

**关键设计点**：
- {为什么这样设计}

## 和其他类的关系
- **被谁调用**: `{CallerClass.method()}` — {场景}
- **调用了谁**: `{CalleeClass.method()}` — {目的}

## 设计决策

### {用问题描述决策点，如：为什么用继承而不是组合？}
- **选择**: {作者的方案}
- **原因**: {[确认]/[推断]} {分析原因}
- **替代方案**: {方案B} — {为什么没选}
- **权衡**: 得到了 {好处}，代价是 {不足}
- **源码依据**: `{File.java:L{n}}`

## 💭 思考题
1. {关于这个类设计决策的思考题，如：为什么作者选择 X 而不是 Y？}
2. {关于替代方案的思考题，如：如果用 Y 代替 X，会带来什么问题？}
3. {关于扩展性的思考题，如：如果需求变成 Z，这个设计还够用吗？}
